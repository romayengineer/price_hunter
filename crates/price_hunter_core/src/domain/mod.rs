//! HTML detection layer: the scraper-based grid pipeline. Pure record types
//! (`Product`, `Detection`, …) live in `price_hunter_domain::model`; pure
//! matching/text helpers live in `price_hunter_domain`.

pub mod detect;
