[package]
name = "price_hunter_domain"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
strsim = "0.11"
chrono = { version = "0.4", features = ["clock"] }

[lints]
workspace = true
