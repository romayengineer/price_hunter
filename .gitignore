/ignore

.DS_Store

products.csv
matrix.csv
brands.csv

captures
crates/*/captures

.#*

/draft

# Added by cargo

/target

profiles
crates/*/profiles

# PocketBase runtime data lives OUTSIDE the repo (~/.local/share/price_hunter)
# so a fresh clone / project deletion never loses the database.
