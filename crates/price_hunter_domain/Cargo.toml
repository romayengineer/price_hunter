[package]
name = "price_hunter_domain"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
strsim = "0.11"
chrono = { version = "0.4", features = ["clock"] }
url = "2"

[lints]
workspace = true
