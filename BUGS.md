# Bugs

## No active bugs

TrailBase was replaced by PocketBase (see the migration commit) and the
`trail user add` version-skew bug that used to live here is no longer relevant.
The project no longer uses TrailBase, `trail`, or `traildepot/`.

If you hit a bug, record it here with reproduction steps and the affected
version.
