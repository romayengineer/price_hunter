---
description: Create a fixture-based integration test from a saved HTML fixture (extracts products, updates detect.rs if needed, verifies tests). Pass the fixture path.
---

Load the `new-fixture-test` skill and follow its workflow for the fixture: `$ARGUMENTS`.
